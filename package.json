{
  "name": "Chatz",
  "version": "0.0.1",
  "scripts": {
    "start": "node server/index.js &",
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec -u tdd ./tests/ && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "dependencies": {
    "socket.io": ">= 0.9"
  },
  "devDependencies": {
    "mocha": ">= 1.15.1",
    "sinon": "*",
    "istanbul": ">= 0.1.46",
    "coveralls": "~2.6.0",
    "jshint": "*",
    "mocha-lcov-reporter": "0.0.1"
  },
  "jshintConfig": {
    "node": true,
    "undef": false,
    "unused": true
  }
}
