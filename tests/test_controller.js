var assert = require('assert');

suite('controller', function () {
	var controller = require('./../server/controller');

	test('should execute callback passing a new user', function () {

	});

	test('');
});